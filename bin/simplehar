#!/usr/bin/env node
var result = require('./execSimplehar.js')(process.argv), simplehar;

if(typeof result === 'string')
	console.log(result);
else {
	simplehar = require('../simplehar.js');
	simplehar(result);
}

